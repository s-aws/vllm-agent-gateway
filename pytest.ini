[pytest]
addopts = -p no:cacheprovider -m "not advanced_workflow"
testpaths = tests
markers =
    advanced_workflow: deferred post-L1 workflow coverage, including broad refactor, implementation-prep continuation, and disposable-apply paths.
