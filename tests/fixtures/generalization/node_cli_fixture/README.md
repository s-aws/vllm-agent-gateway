# Node CLI Fixture

Small synthetic JavaScript CLI fixture for multi-repo validation.

It exposes a `node src/index.js` command and reads runtime configuration from `DEFAULT_PROFILE` and `API_BASE_URL`.
