# phase194-readme-locator Example

Natural prompt:

```text
In /mnt/c/agentic_agents, find where the skill authoring pipeline docs explain promotion boundaries. Read only.
```

Expected route:

```text
code_investigation.plan
```

This example is a draft candidate artifact. Keep it under the authoring-pipeline candidate root until a later lifecycle phase installs or promotes it through the approved path.
